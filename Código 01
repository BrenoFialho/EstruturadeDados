def somatorio():                        # Essa linha será definida a função 'somatorio' que não recebe nenhum argumento.

    total = 0                           # Nessa linha definimos a variável 'total' com o valor 0.
    
    for n in range(1, 101):             # Aqui, temos um loop que varia a variável 'n' de 1 até 100, inclusivamente.
         
        termo = 1/n                     # Calculamos 1/n e armazenamos o resultado na variável 'termo'.

        novo_total = total + termo      # Agora, adicionamos o 'termo' ao 'total' e armazenamos o resultado na variável 'novo_total'.

        
        total = novo_total              # Em seguida, atualizamos o valor de 'total' com 'novo_total' para a próxima iteração.
    
    
    return total                        # Depois que o loop termina, retornamos o valor acumulado em 'total'.


resultado = somatorio()                 # Aqui, chamamos a função 'somatorio' e armazenamos o resultado na variável 'resultado'.


print(resultado)                        # Finalmente, imprimimos o valor de 'resultado' na tela.
